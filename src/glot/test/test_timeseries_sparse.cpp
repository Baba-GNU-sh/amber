#include <gtest/gtest.h>
#include <timeseries_sparse.hpp>

TEST(TimeSeriesSparse, test)
{
    TimeSeriesSparse db;

    // db.push_sample(1.0);
    // db.push_sample(2.0);

    // auto a = db.get_sample(0.0, 1.0);
    // EXPECT_FLOAT_EQ(a.average, 1.0);
    // EXPECT_FLOAT_EQ(a.min, 1.0);
    // EXPECT_FLOAT_EQ(a.max, 1.0);
}
